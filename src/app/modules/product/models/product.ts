// export interface Product {
// }

export type Product = {
    id?: number;
    title?: string; // hack typed forms
    price?: number | null; // hack typed forms
    description?: string;
    thumbnail?: string;    
}
